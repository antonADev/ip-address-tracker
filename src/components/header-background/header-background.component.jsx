import React from 'react';
import { SmallBgnContainer } from './header-background.styles';
const HeaderBackground = ({ children }) => {
  return <SmallBgnContainer>{children}</SmallBgnContainer>;
};

export default HeaderBackground;
